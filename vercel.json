{
  "buildCommand": "npx prisma generate && npx prisma db push --accept-data-loss && node prisma/seed.js && npx next build",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": "nextjs"
}
